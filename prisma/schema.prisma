// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// generator client {
//   provider = "prisma-client-js"
// }
generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Store {
  id        String   @id @default(uuid())
  name      String
  slug      String
  userId    String
  createdAt DateTime @default(now())
  updateAt  DateTime @updatedAt
  billboards      Billboard[] @relation("StoreToBillboard")
  sizes           Size[]     @relation("StoreToSize")
  color           Color[]     @relation("StoreToColor")
  categories      Category[] @relation("StoreToCategory")
  option          Option[] @relation("StoreToOption")
  optionItem      OptionItem[] @relation("StoreToOptionItem")

  @@index([userId, name])
  @@unique([userId,name])
  @@unique([userId,slug])
}

model Billboard {
  id          String @id @default(uuid())
  name        String
  slug      String
  storeId     String
  store       Store @relation("StoreToBillboard", fields: [storeId], references: [id])
  imgUrl      String
  categories  Category[] @relation("BillboardToCategory")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([storeId, name])
  @@index([storeId, slug])
  @@unique([storeId,name])
  @@unique([storeId,slug])
}

model Category {
  id          String      @id @default(uuid())
  name        String
  slug        String

  storeId     String      // Foreign Key to Store
  store       Store       @relation("StoreToCategory", fields: [storeId], references: [id])

  billboard   Billboard   @relation("BillboardToCategory",fields: [billboardId], references: [id])
  billboardId String      // Foreign Key to Billboard

  SizeList    Size[]      @relation("SizeToCategory")
  ColorList   Color[]     @relation("ColorToCategory")

  Option      Option[]    @relation("CategoryToOption")
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@index([storeId,name])
  @@index([billboardId])

  @@unique([storeId,name])
  @@unique([storeId,slug])
}

model Option {
  id          String      @id @default(uuid())
  name        String
  datatype    String
  value       OptionItem[] @relation("OptionItemToOption")
  slug        String

  storeId     String      // Foreign Key to Store
  store       Store       @relation("StoreToOption", fields: [storeId], references: [id])

  categoryList  Category[] @relation("CategoryToOption")
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt


  @@index([storeId,name])
  @@unique([storeId,name])
  @@unique([storeId,slug])
}

model OptionItem {

  id          String      @id @default(uuid())
  name        String
  value       String
  slug        String

  storeId     String      // Foreign Key to Store
  store       Store       @relation("StoreToOptionItem", fields: [storeId], references: [id])

  option      Option      @relation("OptionItemToOption", fields: [optionId], references: [id])
  optionId    String
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@index([storeId,name])
  @@index([optionId,name])
  @@unique([optionId,name])

}

model Size {
  id          String    @id @default(uuid())
  name        String
  slug        String
  value       String

  storeId     String    // Foreign Key to Store
  store       Store     @relation("StoreToSize", fields: [storeId], references: [id])

  categoryList  Category[] @relation("SizeToCategory")

  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([storeId])
  @@index([storeId,name])

  @@unique([storeId,name])
  @@unique([storeId,slug])
}

model Color {
  id          String    @id @default(uuid())
  name        String
  slug        String
  value       String

  storeId     String    // Foreign Key to Store
  store       Store     @relation("StoreToColor", fields: [storeId], references: [id])

  categoryList  Category[] @relation("ColorToCategory")

  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([storeId])
  @@index([storeId,name])

  @@unique([storeId,name])
  @@unique([storeId,slug])
}

